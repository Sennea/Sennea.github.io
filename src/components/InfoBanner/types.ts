export interface InfoBannerPropTypes {
    name: string,
    role: string,
    info: string;
    facebook: string;
    github: string;
    linkedin: string;
    landscape: boolean;
}

export interface ComponentPropTypes {
    landscape: boolean;
}
